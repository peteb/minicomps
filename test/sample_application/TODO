* write multithreading tests. Also, how will they work with intercepts
* locking intercepts
* can we make intercepts show up in the sequence diagram
* can we create a coroutine using `failure`
* verify a publisher for an event exists
